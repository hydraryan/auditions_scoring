########################################
# Local development .env template
# Copy to .env at the project root
########################################

# --- Server (local) ---
PORT=4001
MONGODB_URI=mongodb://127.0.0.1:27017/auditions_scoring
# Generate a long random secret for JWT signing before use
# Example (PowerShell):
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=38393cb97600730933c1c788f47ac698e2a1852418952335f138c2dc03e10a2c

# App users (seeded on first run if missing)
ARYAN_PASSWORD=aryan123
KUNAL_PASSWORD=Kunal@9719

# Public ingest token for Google Forms -> server
# Set to a long random string and use it in the Apps Script header X-Ingest-Token
INGEST_TOKEN=replace-with-random-token

# --- Client (local) ---
VITE_API_BASE=http://localhost:4001

########################################
# Deployment notes (do NOT commit real secrets)
# - On your server host (Render/Railway), set these as environment variables:
#     MONGODB_URI=<your MongoDB Atlas connection string>
#     JWT_SECRET=<long random string>
#     ARYAN_PASSWORD=<set a strong password>
#     KUNAL_PASSWORD=<set a strong password>
# - On Vercel (client project), set:
#     VITE_API_BASE=https://<your-backend-domain-or-render-url>
########################################
